import React from "react";
import { BrowserRouter as Router, Route, Link } from "react-router-dom";
const Landing = () => {
    return ('This is your landing page/registration page with a sign up section for new users')
}

export default Landing;